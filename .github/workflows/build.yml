name: Build

env:
  JVM_VERSION: '21'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: "workflow = ${{ github.workflow }}, ref = ${{ github.event.ref }}, pr = ${{ github.event.pull_request.id }}"
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    name: "Build Deep Dive"
    services:
      ollama:
        image: ollama/ollama
        ports:
          - 11434:11434
        options: --name ollama --rm
    steps:
      - uses: actions/checkout@v4

      - name: Pull Ollama models
        run: docker exec ollama bash -c "ollama pull llama3.2 && ollama pull llava:7b && ollama pull snowflake-arctic-embed && ollama ls"

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JVM_VERSION }}
          distribution: temurin
          cache: maven

      - name: Maven Build
        run: |
          ./mvnw -B -T 1C clean verify \
            -Dquarkus.http.host=0.0.0.0 \
            -Dquarkus.langchain4j.timeout=5m \
            -Dquarkus.langchain4j.openai.base-url=http://localhost:11434/v1 \
            -Dquarkus.langchain4j.openai.chat-model.model-name=llama3.2 \
            -Dquarkus.langchain4j.openai.image-model.model-name=llava:7b
